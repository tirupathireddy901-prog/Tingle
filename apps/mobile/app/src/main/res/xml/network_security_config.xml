<?xml version="1.0" encoding="utf-8"?>
<!--
  Local development talks to a docker-compose stack over plain HTTP/WS
  (no TLS cert for localhost/10.0.2.2). Cleartext is scoped to exactly
  those dev hosts; every other domain — i.e. your real production API
  host — must use HTTPS/WSS. Delete the cleartext domain-config block
  entirely for release builds that point at a real, TLS-terminated host.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
